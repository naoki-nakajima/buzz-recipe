<%= form_with(model: @shop_commitment, url: shop_admin_shop_commitments_path(current_shop_admin.id), local: true)  do |f| %>
  <%= f.text_field :text %>
  <%= f.fields_for :photos, @shop_commitment.build_photo do |i| %>
    <%= i.file_field :image %>
  <% end %>
  <%= f.submit %>
<% end %>
